⚡ CF SCANNER v3.0
V2Ray / Xray کالدفلر + تستر واقعی IP اسکنر
ساخته شده توسط عرشیا کوهیان محمد آبادی
؟CF Scanner چرا ��
بیشتر اسکنرهایی که توی گروهها و کانالها دست به دست میشن،
خام. همین. بعدش IP فقط یه کار بلدن: پینگ گرفتن از یه مشت
توی کانفیگت جاش کنی، تست ،IP به IP ،خودت باید بنشینی دستی
IP کنی، ببینی کار میکنه یا نه — کاری که برای پیدا کردن ۲۰ تا
.سالم میتونه نیم ساعت وقتت رو بگیره
یه ابزار دیگه نیست؛ یه خط تولید کامل کانفیگ CF Scanner
:است
های تمیز رو پیدا کن → خودش برات، روی IP → پینگ بگیر
یه ،SNI/path/پسورد/UUID همون کانفیگ خودت، با همون
بسازه → کپی کن، بچسبون توی IP کانفیگ جدا برای هر
V2RayNG/NekoBox، تمام.
رو جابهجا کنی. دیگه الزم نیست IP دیگه الزم نیست خودت دستی
به یک دامنه یا یک سرویسدهنده خاص محدود باشی. و مهمتر از
زندهست«، واقعًا منظورش این IP همه — وقتی اسکنر میگه »این
نیست که فقط پورت بازه؛ تا جایی پیش میره که از داخل خود
.میگیرد HTTP یک اتصال واقعی برقرار میکند و جواب Xray
همهی قابلیتها ✅
«تب »کانفیگ ��
،VLESS :پارس کردن مستقیم لینکهای اشتراکگذاری
VMess، Trojan
، ws ، grpc ، h2 :پشتیبانی کامل از انواع شبکه
httpupgrade ، splithttp ، tcp
از پشتیبانی TLS و Reality (fingerprint، shortId،
publicKey، spiderX)
خودکار تشخیص SNI، path، host header، ALPN،
allowInsecure
قابل مشاهده ،Xray استاندارد JSON تبدیل خودکار لینک به
و ویرایش
JSON ذخیره/بارگذاری کانفیگ بهصورت فایل
کار با هر دامنهای — نه فقط دامنههای پشت کالدفلر
«Ping تب »تست ��
نه فقط رنجهای) CIDR به فرمت IP وارد کردن هر رنج
(کالدفلر — رنج هر سرویسدهندهای که بخوای
دکمههای آماده برای رنجهای پرکاربرد کالدفلر
و Thread با تعداد (Multi-thread) اسکن چندنخی
قابل تنظیم Timeout
اسکنشده )انتخاب تصادفی از رنج برای IP محدودسازی تعداد
سرعت بیشتر(
نمایش زندهی نتایج با رنگبندی بر اساس سرعت )سبز/زرد/
قرمز(
Ping مرتبسازی خودکار بر اساس کمترین
در هر IP یک) های تمیز در یک فایل تمیز و ساده IP ذخیرهی
(خط
های تمیز بر اساس IP ساخت خودکار کانفیگ برای تمام
IP کانفیگی که در تب »کانفیگ« وارد کردی — مثًال اگر ۲۰
ها ساخته میشود، IP سالم پیدا شد، ۲۰ کانفیگ جدا با همان
(SNI، path ،پسورد ،UUID) بدون اینکه هیچ پارامتر دیگری
تغییر کند
کپی همهی کانفیگهای ساختهشده در یک کلیک به کلیپبورد
برای استفاده بعدی txt ذخیرهی همهی کانفیگها در یک فایل
یا اشتراکگذاری
IP در جدول نتایج → کانفیگ همان IP دوبار کلیک روی هر
بهتنهایی فورًا در کلیپبورد کپی میشود
«V2Ray / Xray تب »تست ��
ping نه فقط) IP در پسزمینه برای هر Xray اجرای واقعی
(!ساده
routing + محلی inbound SOCKS5 ساخت خودکار
صحیح
از طریق پروکسی، بدون نیاز به HTTP تست واقعی اتصال
یا ابزار جانبی curl
مدیریت خودکار پورتهای آزاد )بدون تداخل، حتی در تست
پشتسرهم(
SNI فیلد »دامنه تست« اختصاصی — اگر دامنه واقعی با
فرق داشت یا دامنهات پشت کالدفلر نبود، خودت میتونی
کنی override
الگ رنگی و زماندار از نتیجهی هر تست
یا بهصورت دستی Ping مستقیم از نتایج تب IP وارد کردن
ذخیرهی کامل الگ تست در فایل
بدون پاپآپ) در ویندوز Xray مخفیسازی پنجرهی
(cmd آزاردهندهی
ظاهر و تجربهی کاربری ��
تم اختصاصی سایبرپانک نئونی — فیروزهای، سبز نئون،
صورتی/بنفش نئون روی پسزمینهی مشکی
رابط کاربری تببندیشده و تمیز، مناسب کاربر تازهکار و
حرفهای
نوار وضعیت زنده با درصد پیشرفت اسکن
مقایسه با اسکنرهای قدیمی ️⚔
اسکنرهای
معمولی
CF Scanner
v3.0
Xray تست واقعی با
فقط ❌
ping
اتصال واقعی از ✅
داخل پروکسی
ساخت کانفیگ برای
های پیداشده IP
،دستی ❌
IP به IP
خودکار، همه با ✅
هم، یک کلیک
اسکنرهای
معمولی
CF Scanner
v3.0
محدود به یک دامنه/
سرویسدهنده
هر دامنه، هر ❌ معموًال بله
IP رنج
کپی سریع کانفیگ تک
IP
ندارد
فقط دوبار ✅
کلیک
پشتیبانی پروتکل
معموًال فقط
یکی
✅ VLESS،
VMess، Trojan
ساختیافته برای
Reality
کامل ✅ بعضًا
نصب و اجرا ��
پیشنیازها
Python 3.8 باالتر یا → python.org
→ xray.exe فایل :V2Ray برای تب
github.com/XTLS/Xray-core
اجرا در ویندوز
run.bat
:یا مستقیم
python main.py
راهنمای سریع استفاده ��
خودت رو vless/vmess/trojan در تب »کانفیگ«، لینک .1
کن و »تجزیهی کانفیگ« رو بزن paste
مال خودت یا یکی از) CIDR برو، رنج «Ping به تب »تست .2
رو انتخاب کن و »شروع اسکن« بزن (رنجهای آمادهی کالدفلر
های IP وقتی اسکن تموم شد، یا »�� کپی کانفیگ برای .3
تمیز« رو بزن )همهی کانفیگها با هم کپی میشن(، یا روی هر
که خواستی دوبار کلیک کن )فقط همون یکی کپی میشه( IP
/ V2RayNG کانفیگهای کپیشده رو مستقیم توی .4
هر کالینت دیگه پیست کن / NekoBox
نکات ️⚠
پیادهسازی با) نیست curl نیازی به V2Ray برای تست
(خام socket
باید در همان پوشه یا با مسیر کامل مشخص xray.exe
شود
پورت محلی پیشفرض: 10808
شدهpaste ساخت دستهجمعی کانفیگ فقط برای لینکهای
JSON کار میکند، نه برای فایل (vless/vmess/trojan)
بارگذاریشده
ساخته شده توسط عرشیا کوهیان محمد ⟧<"p align="center>
<p/>⟦ آبادی
